export const ARBITRARY_CURLY_RE = /^(.+)-\{\{([^}]+)\}\}$/;
export const ARBITRARY_BRACKET_RE = /^(.+)-\[([^\]]*)\]$/;
export const ARBITRARY_PAREN_RE = /^(.+)-\(([^)]+)\)$/;

export const ENV_PREFIX = 'env-';
export const ENV_PREFIX_LENGTH = 4;
